---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: etcdbackups.etcd.dfy.io
spec:
  group: etcd.dfy.io
  names:
    kind: EtcdBackup
    listKind: EtcdBackupList
    plural: etcdbackups
    singular: etcdbackup
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EtcdBackup is the Schema for the etcdbackups API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: EtcdBackupSpec defines the desired state of EtcdBackup
            properties:
              etcdUrl:
                description: etcd集群是一个分布式系统，由多个节点相互通信构成整体的对外服务， 每个节点都存储了完整的数据，并且通过Raft协议保证了每个节点维护的数据都是一致的。
                  https://zhuanlan.zhihu.com/p/87014600 因此只需要备份一个节点即可
                type: string
              oss:
                properties:
                  ossSecret:
                    description: 'Secret Object: AcessKey AccessSecret'
                    type: string
                  path:
                    type: string
                required:
                - ossSecret
                - path
                type: object
              s3:
                properties:
                  endpoint:
                    type: string
                  path:
                    type: string
                  secret:
                    description: 'Secret Object: AcessKey AccessSecret'
                    type: string
                required:
                - endpoint
                - path
                - secret
                type: object
              storageType:
                description: 为什么重命名？ 因为易读 具有含义
                type: string
            required:
            - etcdUrl
            - storageType
            type: object
          status:
            description: EtcdBackupStatus defines the observed state of EtcdBackup
            properties:
              completionTime:
                format: date-time
                type: string
              phase:
                description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                  of cluster Important: Run "make" to regenerate code after modifying
                  this file'
                type: string
              startTime:
                format: date-time
                type: string
            required:
            - phase
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
